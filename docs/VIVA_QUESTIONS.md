Viva Questions and Short Answers
What is Spring Boot? Spring Boot simplifies Spring application development using auto-configuration, starters and embedded servers.
Why use REST API? REST provides a standard HTTP-based interface between frontend and backend.
What is JPA? Java Persistence API is a specification for mapping Java objects to relational database tables.
Why MySQL? It is a popular relational database suitable for structured application data.
What is JWT? JSON Web Token is used here to authenticate API requests without server-side HTTP sessions.
What is @Entity? It tells JPA that a Java class represents a database entity.
What is @ManyToOne? It represents a many-to-one relationship, such as many appointments belonging to one doctor.
What is CORS? Cross-Origin Resource Sharing controls browser requests between different origins.
What is Postman? A tool used to send HTTP requests and test REST APIs.
How is double booking prevented? Before saving an appointment, the backend checks whether the same doctor already has an appointment at the same date and time.
